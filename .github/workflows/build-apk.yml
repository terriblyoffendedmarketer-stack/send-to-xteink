name: Build Android APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'android-wrapper/**'
      - '.github/workflows/build-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-wrapper

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Copy APK for serving
        working-directory: .
        run: cp android-wrapper/android/app/build/outputs/apk/debug/app-debug.apk public/send-to-xteink.apk

      - name: Commit updated APK
        working-directory: .
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/send-to-xteink.apk
          git diff --cached --quiet && echo "No APK changes" || (git commit -m "Update built APK [skip ci]" && git push)

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: send-to-xteink-debug
          path: android-wrapper/android/app/build/outputs/apk/debug/app-debug.apk
